name: Zipper

on:
  workflow_dispatch:
    inputs:
      filetozip:
        description: 'File to download and zip'
        required: true


jobs:
  zipFile: 
    runs-on: ubuntu-latest
    steps:
      - name: Download a file
        run: curl ${{ github.event.inputs.filetozip }} -o temp
      - uses: montudor/action-zip@v1
        with:
          args: zip -qq -r output.zip temp
      - uses: actions/upload-artifact@v2
        with:
          name: output.zip
          